# Rappel Git Push

1. "Sélectionner" les fichiers à envoyer    
`git add *fichier*`    
`git add .` pour ajouter tout le contenu du dossier où tu te trouves

2. Créer un commit
`git commit -m "message"`    
Il **FAUT** taper un message, sinon le commit ne se fera pas

3. Push Push Push
`git push`